import { Container } from "@mui/material";
import { Box } from "@mui/system";
import React from "react";

const Exchanges = () => {
  return <Container size='sm'>exchanges </Container>;
};

export default Exchanges;
